import { DatePiplePipe } from './date-piple.pipe';

describe('DatePiplePipe', () => {
  it('create an instance', () => {
    const pipe = new DatePiplePipe();
    expect(pipe).toBeTruthy();
  });
});
